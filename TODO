TODO: 
Middlewear to check REDIS connection before starting
Fix regex on add journal function JOI
Add update ISSN function (Grab latest integrity issn-doi check and grab DOIS, if not one schedule one) - So can be ran as a cron job.
Centralise logging to /logger (see src/app/queues/jobLogger)
Create tests for functions (API routes, functions etc).
Convert all files the TS.
Create DATA ISSN integrity check (What percentage has missing attributes etc).

